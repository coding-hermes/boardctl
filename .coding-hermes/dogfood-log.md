# boardctl dogfood log

2026-09-04 | verdict=PROMISING-BUT-ROUGH | promise="A user can manage a coding-hermes foreman board (tasks/events/board/fixtures JSONL) with a fast, database-free CLI — create/update tasks, append audit events, read stats/header, validate/doctor integrity — with byte-stable diffs and exit codes 0/1/2." | install_seconds=4 (release binary, bare Debian 13 bunker, no sudo/no Go) + go-install path 16s | bunker=las-bunker-03 agent=16af6585 | smoke=ok (release binary: stats/validate/doctor/list on cloned repo's own board, all exit 0; go-install build ran version probe) | ttfs=blocked-on-new-project (reads OK anywhere in seconds; writes impossible on empty boards until BT-005 `init`) | friction=9 | top findings: BT-005 no board bootstrap (create fails on every empty-board state, stale DuckDB-era error text), BT-006 -C .coding-hermes resolution + exit-code contract leak, BT-007 write vocabulary unenforced (guard/ci/priority/depends-on/negative counters) with validate silent on junk rows | also: BT-008 v0.1.1 missing sha256sums.txt, BT-009 docs order, BT-010 topology-B writes dead-end | findings filed WITH boardctl itself on its own board (self-hosting proof) | BT-011 (follow-up commit e79f632): create/update never append events — README promises task_created/completion events that the code never writes (AppendEvent only reachable via `event` subcommand); audit-trail silent hole for every consumer
