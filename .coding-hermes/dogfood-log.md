# boardctl dogfood log

2026-09-04 | verdict=PROMISING-BUT-ROUGH | promise="A user can manage a coding-hermes foreman board (tasks/events/board/fixtures JSONL) with a fast, database-free CLI — create/update tasks, append audit events, read stats/header, validate/doctor integrity — with byte-stable diffs and exit codes 0/1/2." | install_seconds=4 (release binary, bare Debian 13 bunker, no sudo/no Go) + go-install path 16s | bunker=las-bunker-03 agent=16af6585 | smoke=ok (release binary: stats/validate/doctor/list on cloned repo's own board, all exit 0; go-install build ran version probe) | ttfs=blocked-on-new-project (reads OK anywhere in seconds; writes impossible on empty boards until BT-005 `init`) | friction=9 | top findings: BT-005 no board bootstrap (create fails on every empty-board state, stale DuckDB-era error text), BT-006 -C .coding-hermes resolution + exit-code contract leak, BT-007 write vocabulary unenforced (guard/ci/priority/depends-on/negative counters) with validate silent on junk rows | also: BT-008 v0.1.1 missing sha256sums.txt, BT-009 docs order, BT-010 topology-B writes dead-end | findings filed WITH boardctl itself on its own board (self-hosting proof) | BT-011 (follow-up commit e79f632): create/update never append events — README promises task_created/completion events that the code never writes (AppendEvent only reachable via `event` subcommand); audit-trail silent hole for every consumer

2026-09-12 | verdict=SHIPPABLE | promise="A user can manage a coding-hermes foreman board (tasks/events/board/fixtures JSONL) with a fast, database-free CLI — create/update tasks, append audit events, read stats/header, validate/doctor integrity — with byte-stable diffs and exit codes 0/1/2." | install_seconds=3 (README quickstart verbatim on bare Debian 13.6 bunker, checksum-verified v0.1.2) | bunker=las-bunker-03 agent=fcfd3f4b (destroyed) | smoke=ok (validate+doctor OK on cloned repo board; init->create->update smoke OK in mktemp) | ttfs=<5s (init to validated board, local scratch) | friction=7 | re-run at HEAD 237e594: BT-005..019 fixes verified live (init 4-file bootstrap, create/update audit events, event --tick header auto-sync = BT-014 closed, byte-stable 1-line diff on 186-row scheduler board copy) | top findings: BT-023 create --id accepts 'bad id!' and validate stays silent (id vocab gap), BT-024 update --commit-hash hijacks header last_commit drift pointer + header updated_at stays stale, BT-025 real foreman rows todo/done/open fail validate (23 errors on live scheduler board), BT-026 legacy/partial board error UX (tasks.jsonl-only dir = 'no board found' exit 2; pretty-printed = raw json error) | docs: docs/dogfood/2026-09-12-integration.md + diagnostics addendum + skill refreshed to 1.2.0

2026-09-20 | verdict=PROMISING-BUT-ROUGH (report surface; CLI/board core still SHIPPABLE) | promise="A user can turn any board into a self-contained multi-board HTML analytics report (render), upload boards through a loopback browser server (serve), and round-trip the payload back into a board (import) with no network and no writes to board files." | install_seconds=3 (README quickstart verbatim on bare Debian 13.6: checksum-verified v0.1.6 release binary, no Go, no sudo) | bunker=las-bunker-03 agent=1cbc343f (destroyed, key removed) | smoke=ok (version v0.1.6; on the SAME box: clone ab0489d, doctor/validate OK, init->create->update OK, render 315821 bytes with 0 external refs, serve HTTP 200 + /api/boards, import round-trip no-op) | ttfs=~0s (install 3s, render 0.016s, first real command instant) — time-to-TRUST is where it fails | goal_board_not_found_exit=2 (contract holds) | angle=render/serve/import — the analytics stack runs 4 and 12 never touched | friction=3 blocking (burndown blank, footnote misattributed, folder-upload unverifiable) + 1 honesty + 2 UX | top findings: DF-BOARDCTL-1 (P1) burndown chart NEVER renders — template.go:714 reads d.burndown.days but the payload's burndown is {window,open} while burnup is {window,days,cum}; chartLines bails on empty days so the panel shows "no tasks yet" on a 42-task board and discards 17 data points — proven live in-browser AND on a fresh bunker box; DF-BOARDCTL-5 (P1) completed_no_done_ids is a payload-level union across all boards so multi-board serve reports attribute another board's uncomputable rows to every board (b2's CHT-* ids shown in the boardctl view); DF-BOARDCTL-6 (P1) the README's first serve path (folder upload) could NOT be exercised end-to-end — filed as a falsifiable row with acceptance criteria, NOT a silent pass | also: DF-BOARDCTL-2 serve accumulates every upload forever (no dedupe/reset; -C board double-registered; compare table diffs a board against itself), DF-BOARDCTL-3 report parser rejects the -0500 dialect boardctl's own detector accepts+preserves (4 rows/1 board today, whole-board time series at risk), DF-BOARDCTL-4 velocity silently reports 199 of crier's 391 completions (182 missing ones DO have task_completed events) | VERIFIED CLEAN: self-contained/no-network (0 external refs), read-only claim (sha256 identical after render + serve upload), loopback-only refusal exit 2, import name-mismatch refusal, id-format guard, fixture exclusion correct | rows DF-BOARDCTL-1..6 appended (verified at tail; board validate OK, 49 tasks/161 events) | infra: 1 spawn OK, 0 500s (agent 1cbc343f), destroyed cleanly | left behind: docs/dogfood/2026-09-20-render-serve-integration.md, diagnostics.md Run-8 addendum, skills/boardctl-usage bumped to 1.4.0 (report-surface section + pitfalls), this entry
